{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/config/sentences_mode_config.json",
  "title": "SentencesModeConfig",
  "description": "Schema contract for the SentencesModeConfig frozen dataclass governing multi-sentence processing mode parameters.",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether multi-sentence processing mode is active."
    },
    "min_sentences": {
      "type": "integer",
      "minimum": 1,
      "default": 2,
      "description": "Minimum number of sentences required to activate multi-sentence alignment mode."
    },
    "alignment_method": {
      "type": "string",
      "enum": ["auto", "proportional", "sequential", "newline_join", "proportion"],
      "default": "auto",
      "description": "Algorithm used to align source and target sentence sequences. Supported: 'auto', 'proportional', 'sequential', 'newline_join', 'proportion'."
    },
    "spawn_order": {
      "type": "string",
      "enum": ["normal", "reversed", "reverse"],
      "default": "normal",
      "description": "Order in which spawned sentence-level processing tasks are dispatched."
    },
    "parent_mode": {
      "type": "string",
      "enum": ["full", "summary", "stub", "none"],
      "default": "full",
      "description": "How the parent sentence context is represented in multi-sentence output."
    },
    "multi_mode_decompose": {
      "type": "boolean",
      "default": false,
      "description": "Whether multi-mode processing decomposes each sentence independently before combining results."
    },
    "legacy_spawn_children": {
      "type": "boolean",
      "default": false,
      "description": "Whether to forcibly spawn child windows in multi-mode even when sentences_mode is disabled."
    },
    "deduplication_scope": {
      "type": "string",
      "enum": ["sentence", "global", "none"],
      "default": "sentence",
      "description": "Scope at which deduplication is applied: 'sentence' (per-sentence local), 'global' (cross-sentence combined), or 'none'."
    },
    "delivery_mode": {
      "type": "string",
      "enum": ["container", "multi_window"],
      "default": "container",
      "description": "Delivery format for sentences mode: 'container' (in-page tabs in single workspace container) or 'multi_window' (multi-window / multi-tab cascade)."
    },
    "tab_bar_position": {
      "type": "string",
      "enum": ["top", "bottom", "inline"],
      "default": "top",
      "description": "Tab bar position in container mode: 'top' (fixed motionless at top of viewport with glassmorphism), 'bottom' (fixed motionless directly above action toolbar), or 'inline' (in document flow at top of container)."
    }
  },
  "required": [
    "enabled",
    "min_sentences",
    "alignment_method",
    "spawn_order",
    "parent_mode",
    "multi_mode_decompose",
    "legacy_spawn_children",
    "deduplication_scope",
    "delivery_mode",
    "tab_bar_position"
  ],
  "additionalProperties": false
}
